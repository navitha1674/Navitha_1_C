#include <stdio.h>

int main() {
    int arr[3] = {10, 20, 30};
    printf("First element: %d\n", arr[0]); // Accessing an element
    arr[1] = 50; // Modifying an element
    printf("Modified second element: %d\n", arr[1]);
    return 0;
}
